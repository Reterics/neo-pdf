import React from 'react';
import {NeoPDF} from '../../../dist';

const App: React.FC = () => {
    return (
        <div>
            <h1>TypeScript Example of MyComponent</h1>
            <NeoPDF />
        </div>
    );
};

export default App;
